name = "groq-perf-proxy"
main = "index.js"
compatibility_date = "2024-01-01"

# After running `wrangler deploy`, set the secret with:
#   wrangler secret put GROQ_API_KEY
# The key is stored encrypted in Cloudflare — never in this file.
